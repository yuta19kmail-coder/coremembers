# CoreMembers 引き継ぎメモ

最終更新：2026-06-17 ／ 現行コードバージョン：**v0.7.3**

> 🆕 **v0.7.3（2026-06-17）＝ スキル管理の得意度/運転メモ撤去＋廃止キー掃除**
> - スキル管理「🚗 乗れる車」タブの付帯列から **得意度(drivingConfidence)・運転メモ(drivingNotes) を撤去**（残るは「近場のみ」のみ）。`driveExtraCols` 簡素化。
> - **廃止された乗れる車キー（旧 `big1`/`big2`/`left` 等）の掃除**：`driveCaps` のうち現行 `DRIVE_SKILLS` に無いキーは **表示しない（`memberSkillSummaryHTML`）＋スキル管理で保存した時に自動削除（`skillSaveEdit`）**。旧データで社員モーダルのサマリーに「big1」が出ていた件の対策。※社員モーダルの能力サマリー自体は v0.7.2 で撤去済みなので、最新版を出せばモーダルには出ない。
> - 版3か所 0.7.3。Firestore/ルール変更なし。

> 🆕 **v0.7.2（2026-06-16）＝ 社員モーダルのスリム化**：モーダルから **週何日(weeklyDays)／連絡先(ext・mobile・emailWork)／生活圏(homeArea)／能力サマリー(表示)／スキル・資格(certs・skillTags・strengths・weaknesses)** のUIを撤去。`saveMemberForm` の該当読み取りも削除（**データ自体は消さず ME 現値を保持**＝既存値は残る）。残る編集項目＝基本/組織(部・課・上長・兼務・上位役割)/雇用(雇用形態・在籍・入社日・予定シフト出す・配車対象)/アプリ連携。スキルは「🧩 スキル管理」で編集。
> - ✅ **方針確定（2026-06-16）**：稼働日（週何日）等のざっくり値は CoreMembers に持たせない。**正確なシフトは MHS に直接流し込む**運用にする（戻さない）。AI 配車/シフトの稼働情報は MHS のシフトが真実。現状 MHS は coreMembers をまだ読んでいない（portalMembers でログイン判定のみ）＝今回の撤去で影響なし。

> 🆕 **v0.7.1（2026-06-16）＝ 部署ビューを課で小グループ化／並び替えはボタン式／スキル列見出し折返し**
> - **部署ビューで課の小グループ表示**：部見出しの中で、課がある部は「課なし（部直属）→ 各課の小見出し＋メンバー」に分割（`dept-sub`）。課が無い部は従来どおりフラット。
> - **並び替えは「↕ 並び替え」ボタンで入るモード式**に（常時▲▼をやめた）。ON中だけ各カードに▲▼が出て「✓ 並び替え終了」で抜ける（`memReorder`/`toggleMemReorder`）。検索中は不可。
> - **並び替えスコープを階層対応**：一覧=全体(`all`)／部署ビュー=その人が居る最小グループ（`sec:課ID`／`deptnosec:部ID`＝課なし／`dept:部ID`＝課が無い部／`_none`＝未所属）。`_inScope` で判定。入替は sortOrder 交換＝全画面共通順に反映。
> - **スキル管理の列見出しを折返し可に**：`.skmx thead .sk-col{white-space:normal;max-width:96px;…}`。「3ナンバーミニバン（…）」等の長いラベルが2〜数行で折返り、チェックだけの列が無駄に広がらない。
> - 変更：`index.html` のみ。**Firestore/ルール変更なし**。版3か所 0.7.1。

> 🆕 **v0.7.0（2026-06-16）＝ 名簿の一覧/部署トグル＋全画面共通の並び順**
> - **全画面共通の手動並び順 `sortOrder`（数値）を導入**。`_cmpOrder`（sortOrder優先→無ければ名前順）で `subscribeMembers` のソートを名前順から共通順に変更。これで**スキル管理・名簿・部署ビューすべて同じ順**になる（従来：各画面が名前順でバラついていた＝「平から始まって途中で社長」問題の解消）。
> - **社員名簿に「部署／一覧」トグル**（`memView`・`setMemView`）。部署＝部ごとにグループ（課バッジ）、一覧＝共通順のフラット表示。
> - **▲▼で並び替え**（管理者・`memberMove(id,dir,scope)`）。一覧＝全体スコープ、部署ビュー＝その部署内で入替（同じ`sortOrder`を交換）。初回操作時に全員へ番号付与（`_ensureOrders`）。検索中は並び替え非表示（部分集合の誤操作防止）。並び替えは即保存され全画面に反映。
> - 変更：`index.html`（renderMembers/memberCardHTML 改修・reorderBtns・CSS .memview-*/.reord/.ordbtn）。**Firestore/ルール変更なし**（coreMembers に sortOrder 追加のみ）。版3か所 0.7.0。
> - ⏭ 補足：並び替えは現状 ▲▼（ドラッグは未対応・必要なら次段）。exec strip（組織の上位役割）は topRole 順のまま（意図的）。

> 🩹 **v0.6.2（2026-06-16）＝ toast の致命バグ修正**：`#cm-toast` は**CSSだけ定義され要素(div)が body に無かった**ため、`toast()` が毎回 `Cannot set properties of null` で落ちていた（Promise内は黙殺、クリックハンドラ内＝スキル管理「保存して終了」で表面化し処理停止）。`toast()` を**要素が無ければ自動生成する自己修復型**に変更。これで保存も通る。版3か所 0.6.2。

> 🩹 **v0.6.1（2026-06-16）＝ スキル管理「保存して終了」の堅牢化**：①保存対象を EDIRTY 頼みから「作業コピー(EBUF)と本体の差分」で確定（変更取りこぼし防止・`_skillSnap`）②保存失敗時はエラー内容を `alert` で表示し**編集モードを維持**（編集を失わない）③保存ボタンを保存中は無効化。版表記3か所を 0.6.1 に。`firebase deploy --only hosting:coremembers` のみ。

> 🆕 **v0.6.0（2026-06-16）＝ 社員に「部＋課」を同時設定できるように**
> - 社員編集の「組織」を **所属（部）＝最上位部署（親なし）** と **課＝その部の直下（カスケード・任意）** の2段に。部を選ぶと課セレクトが下位部署だけに切り替わる（`meDeptChange`／`sectionSelectHTML`／`sectionOptionsHTML`）。
> - 新フィールド `sectionDeptId`（課）を追加（`primaryDeptId`＝部のまま）。**旧データ正規化**：主所属に課（親を持つ部署）が入っていたら、開いた時に 部＝親 / 課＝その課 に読み替え（`memberDeptIds` 共通ヘルパー・表示/グルーピングでも使用）。
> - 名簿は**部でグループ**（従来通り）＋各カードに**課バッジ（🏢）**を表示。課は任意（部だけでもOK）。
> - **バグ修正**：組織画面の上位役割ストリップでアバターが**異常に巨大化**していた件＝アバターCSS（`.mav`/`.mav img`）が `.mcard` 内にしかスコープされておらず、`.execcard` 内の画像にサイズ/`overflow:hidden`が当たらず元サイズで表示されていた。CSSを全体共通（`.mav`）に変更して解消。
> - 変更：`index.html` のみ（v0.6.0・3か所版表記）。データ構造は項目追加だけ＝**Firestore/ルール変更なし → `firebase deploy --only hosting:coremembers` のみ**。
>
> **🆕 同 v0.6.0 後半＝能力（スキル）を専用マトリクスビューに分離**
> - **新ビュー「🧩 スキル管理」**（サイドバー・`view-skills`／`renderSkills`）＝社員（行）×項目（列）のExcel風マトリクスで一括編集。タブ切替＝**乗れる車／免許・MT／登録業務／場所×業務**。セルクリックで即トグル＆`memCol().set(...,{merge:true})`でその場保存（`saveMemberField`）。場所タブは場所ごとに「行ける(✅/🔶/・)」＋その場所の業務チェックを横並び（`skillMatrixLoc`）。退職者は非表示。`goableLocationIds`も派生保存（MHS/PitFlow互換）。
> - **「乗れる車」を設定化**：`coreSettings/app.driveSkills`（`{key,label}[]`）。設定ページに編集UI（`DSEDIT`/`dsAdd`/`dsMove`/`dsDel`/`saveDriveSkills`）。未設定時は `DRIVE_CAPS` にフォールバック。`driveCaps[]` のキーがこのリスト基準に。**レベルを増やしてもコード修正不要**。
> - **社員詳細はスキルを完全読み取り化**：編集モーダルの 乗れる車/特殊運転/免許/登録業務/場所×業務 **＋ 近場のみ/運転得意度/運転メモ** をすべて**畳んだサマリー（`<details>`「▼結果を見る」・`memberSkillSummaryHTML`）**に。編集は全て「🧩 スキル管理」に統合。`saveMemberForm` から `me-mt`/`me-near`/`me-conf`/`me-dnotes` の読みを撤去（ME現値を保持）。詳細で編集が残るのは 生活圏(homeArea) と スキル・資格(certs/skillTags/strengths/weaknesses) の自由記入のみ。
> - **近場のみ/得意度/メモ は 🚗 乗れる車タブの右側に列追加**（得意度=select、近場のみ=✓、運転メモ=テキスト。`skillMatrixSimple` に `extraCols` 対応）。
> - **スキル管理を編集モード制に**：既定は閲覧（セル操作不可）→「✏️ 編集する」で編集モード（作業コピー`EBUF`を編集・本体は触らない）→「💾 保存して終了」で変更社員(`EDIRTY`)だけ一括保存→本体反映。「キャンセル」で破棄。`SKILL_EDIT`/`skillSrc(m)`/`skillEnterEdit`/`skillSaveEdit`/`skillCancelEdit`。トグル/select/textは編集モード時のみ反応・即時保存しない（保存はまとめて1回）。
> - **表示を拡大＋社員名にアバター**：セル40×34px/フォント16px、社員列にアバター(26px)＋名前（`skNameCell`）。
> - **免許タブを削除**（「免許は無し」指示）。`licenses` フィールドは温存だがUIなし。サマリーからも免許行を削除。
> - 追加CSS：`.skmx`（sticky行頭・ヘッダ）/`.skcell`/`.lcell`/`.sk-tab`/`.skde`/`.sumrow` 等。**Firestore/ルール変更なし**（coreMembers/coreSettings は既存ルールで管理者書込可）。全インラインscript構文チェック済み。
> - **自社の「乗れる車」基準（既定12段階・チェック式）を `DRIVE_CAPS` に設定**：軽だけ／国産コンパクトまで／5ナンバーミニバン（ノア・ヴォクシー）／3ナンバーミニバン（アルファード・ヴェルファイア）／ハイエース・キャラバン／スポーツカー系／輸入車（MINIぐらいまで）／3シリーズぐらいまで／5シリーズぐらいまで／ランクル・ゲレンデ等／Vクラス等 大型バン系／LS・Sクラス等 大型ビッグセダン。設定で増減可。運用は「その人が乗れる一番上の段にチェック」想定。旧キー(left/big1/big2)のデータがあれば再入力（マトリクスは新規のためほぼ無し）。
> - **特殊運転（人の可否）タブを追加**：スキル管理に「🛠 特殊運転」タブ＝**左ハンドル／MT／左MT／車高低い** の4つを人ごとにチェック（`SPECIAL_DRIVE`／フィールド＝`canLeft`・`mt`(既存流用)・`canLeftMt`・`canLowCar`）。免許タブからMTは特殊運転へ移動（免許タブは免許のみ）。社員詳細サマリーにも「🛠 特殊運転」行を表示。
> - **⏭ PitFlow側TODO**：同じ4つ（左ハンドル／MT／左MT／車高低い＝リップ注意）を**PitFlowの車両カードに"車両個別"の属性として**付ける。配車マッチング＝「車がその属性」かつ「人がその可否ON」で配車可。CoreMembers側の人の可否はこの特殊運転タブが持つ。

---

## 0. これは何か
小林モータース（自動車整備＋中古車・スタッフ約15名）の **正式な社員名簿＋組織＋スキル＋場所マスター** アプリ。
CoreFlow エコシステムの一員。単一HTMLファイル（インラインCSS/JS）で、house パターンに沿う。

- 本番URL：`https://coremembers.kobayashi-motors.com`
- ファイル：`D:\アプリ開発\CoreMembers\index.html`
- 最終目標（将来 Phase4-5）：AIによる横断的な指導履歴／人物Memoryの集約。**まだそこには行かない。今は土台（名簿・組織・権限・場所・スキル）を固める段階。**

### CoreMembers と portalMembers の関係（重要）
- **CoreMembers＝親**：全スタッフ（ログインしない週2バイトも含む）の人事マスター。
- **portalMembers＝子（アクセス層）**：CoreFlowにログインできる人だけ。`portalMemberId` で紐づけ。
- 「CoreFlowにアクセスできる＝全スタッフではない」。週2バイトは CoreMembers にだけ存在（portalMembers には作らない）。

---

## 1. インフラ / 接続
- Firebase（compat SDK 10.13.2）を **carflow-9d500 プロジェクトに相乗り**。
- Auth：Google ログイン（signInWithPopup）→ portalMembers で入室判定（落ちたら拒否画面）。
- authDomain は専用サブドメイン `coremembers.kobayashi-motors.com`（iOS Safari のログインループ回避）。
- Hosting site：`coremembers-km`（`.firebaserc` の target=coremembers）。
- Firestore コレクション（すべて `companies/{COMPANY_ID}/...` 配下）：
  - `coreMembers` … 社員
  - `coreDepts` … 部署（組織）
  - `coreLocations` … 場所マスター
  - `coreSettings/app` … アプリ設定（場所カテゴリ等）← **v0.4.0 で新設**
  - `userPrefs/{uid}` … uid↔memberId 橋渡し
  - `portalMembers`（読み取り）… ログイン/権限層

### 権限モデル（4段階）
`CM_ROLE` = オーナー / 管理者 / 責任者 / 一般。
- `IS_ADMIN = (オーナー || 管理者)` のみ編集可。
- オーナー＝portalMembers の master/admin。それ以外は portalMembers の coremembers.role を見る。

---

## 2. セキュリティルール（要注意）
- 真実は `D:\アプリ開発\CarFlow\carflow\firestore.rules`。
- CoreMembers 用ブロック：`coreMembers / coreDepts / coreLocations / coreSettings` に
  `allow read: if _isMember(cid); allow write: if _cmCanEdit(cid);`
  - `_cmCanEdit` = admin/manager（オーナー・管理者）のみ。
- **`coreSettings` は v0.4.0 で追加。ルールのデプロイがまだなら必須：**
  ```
  cd D:\アプリ開発\CarFlow\carflow
  firebase deploy --only firestore:rules
  ```

---

## 3. データモデル（主要フィールド）

### coreMembers（社員）
- 基本：`name, kana, ini, roleTitle(現場役職), primaryDeptId, subDeptIds[](兼務), managerId(上長)`
- **`topRole`（上位役割）** … 代表/副代表/専務/常務/取締役/顧問。部より上を“箱”でなく人の役割で表現（v0.5.0）。
- 雇用：`employmentType, status, joinedAt, weeklyDays, showInSchedule, schedulable`
- 連絡先：`ext, mobile, emailWork`
- 運転：`licenses[], mt, driveCaps[](左ハンドル/大型①/大型②), nearOnly(近場のみ), drivingConfidence, drivingNotes`
- 登録業務：`regTasks[]`（中古新規/名義変更/ナンバー変更/住所変更）
- **能力マトリクス：`locSkills = { [locId]: { status:'ok'|'never', tasks:[業務名,...] } }`**
  - status：✅行ける / 🔶行ったことない。tasks：その場所でできる業務（場所側 tasks に✅）。
  - 保存時 `goableLocationIds`（status==='ok'）を派生保存＝MHS/PitFlow互換。
- 生活圏：`homeArea`、スキル：`certs[], skillTags[], strengths, weaknesses`
- 連携：`portalMemberId`、アバター：`photo`（紐づけ時のスナップショット）
- ※ `color`（アイコン色）は **v0.4.1 で廃止**（アバター自動生成に移行）。`drivableSizes` も廃止。

### coreLocations（場所マスター）
- `name, aliases[](通称・AI照合用), category(キー), frequent(⭐よく使う), isBase(🏠会社/基準)`
- 座標：`lat, lng`（**手入力 or Googleマップ座標/URL貼り付けで取得。自動ジオコーディングは廃止**）
- `address(表示・地図リンク用), memo, active, order`
- **`tasks[]`（その場所で発生する業務）** … ＋追加式。一覧では組織図のように枝でぶら下げ表示。
- ※ `area`（手入力方面）は廃止。方面は基準地点(isBase)からの方角・距離を座標から自動算出して表示。

### coreDepts（部署）
- `name, type(部/課/班/拠点), parentId(上位部署), leaderMemberId(責任者), color, order`

### coreSettings/app
- `locCats: [{key,label}, ...]` … 場所カテゴリ（設定ページで編集・並び替え・追加・削除）。
  - 未設定時は `DEFAULT_LOC_CATS`（陸運局/部品商/取引先/自社拠点/顧客先/**警察署**/その他）にフォールバック。

---

## 4. 画面 / 機能（現状）
- **社員名簿**：部署ごとにグルーピング、アバター付きカード、検索、在籍フィルタ、portalMembersから取り込み。
- **組織（部署）**：最上部に「上位役割」ストリップ（代表・役員などを役割バッジで）→ その下に部署ツリー。
- **場所マスター**：並び順＝①自社/基準 → ②⭐よく使う（カテゴリ→五十音）→ ③カテゴリ→五十音。業務を枝でぶら下げ表示。
- **⚙️設定**（v0.4.0新設）：場所カテゴリの名称変更・並び替え・追加・削除。管理者のみ。
- **About**：接続状況・ログアウト。

### アバター仕様（v0.4.1）
- portalMembers 紐づけ済み → **その人の“今の”Googleアバターを引く**（`memberPhoto()` が portalById で最新を参照。アプリ再読込で追従）。
- 紐づけなし（手動追加） → **苗字頭文字＋名前ハッシュ由来の色**でGoogle風アバターを自動生成（`avatarColor()` / `avatarHTML()`）。

---

## 5. UI上の決定事項
- 編集モーダル（社員/部署/場所）は **背景クリックで閉じない**（誤操作で入力消失を防止）。×／キャンセル／保存のみ。
- 右上のロールバッジは表示しない（過去に削除）。

---

## 6. 直近の変更履歴（このセッション）
- v0.2.0 能力マトリクス（場所×業務／運転資格／登録業務）＋氏名入力バグ修正（id衝突 me-name→me-fullname）
- v0.3.0 業務を＋追加式（枝表示）／「よく使う」は場所属性化／方面は座標から自動算出
- v0.3.1 座標をURL/コピー座標対応・モーダル外クリック閉じ無効化
- v0.3.2〜0.3.3 住所ジオコーダ迷走（地理院→誤差）→ **自動取得は廃止、緯度経度の直接貼付に一本化**
- v0.3.4 場所一覧の並び替え（自社→よく使う→カテゴリ→五十音）
- v0.4.0 **設定ページ新設**・場所カテゴリ編集/並替/追加・**警察署追加**（要：coreSettingsルールのデプロイ）
- v0.4.1 アイコン色廃止・アバター自動（portal連携＋苗字頭文字生成）
- v0.5.0 **上位役割を人ベースで表現（箱なし）**・組織図に役割ストリップ・兼任は主所属＋兼務で表現

---

## 7. 未決 / TODO
- **上位役割（topRole）の選択肢を設定ページで編集可能にするか？** → ゆうたに確認中（今は固定リスト）。
- 基準地点（会社）の登録：場所マスターに会社拠点を1件作り `🏠会社/基準` をON＋座標を入れる。これが全場所の方角・距離の起点。未設定時は松戸市五香の暫定座標で計算。
- 将来（Phase4-5）：人物Memory／AI横断指導履歴の集約（`coreMemories/corePatterns` 想定。別途厳格ルール）。

---

## 8. デプロイ手順
ルール（変更した時のみ・CarFlow側）：
```
cd D:\アプリ開発\CarFlow\carflow
firebase deploy --only firestore:rules
```
本体（CoreMembers）：
```
cd D:\アプリ開発\CoreMembers
git add -A
git commit -m "メッセージ"
git push
firebase deploy --only hosting:coremembers
```
※ルールを変えた回は **ルール→本体の順**でデプロイ（逆だと一瞬保存が弾かれる）。

---

## 9. 開発環境の注意（重要）
- **bashマウントは Editツール書き込み後に古い/部分的な内容を返す**ことがある（行数が実ファイルと食い違う）。
  - Readツールが正。`node --check` 等のマウント前提検証は当てにならない。コード監査はReadで行う。
- バージョンは3か所更新：`<meta app-version>` / ログイン画面 `.ver` / トップバー `.ver`。

---

## 10. 関連ファイル
- `D:\アプリ開発\CoreMembers\index.html` … 本体
- `D:\アプリ開発\CoreMembers\firebase-init.js` … authDomain=coremembers...
- `D:\アプリ開発\CoreMembers\firebase.json` / `.firebaserc` … target=coremembers→coremembers-km
- `D:\アプリ開発\CoreMembers\正式開発_設計プラン.md` … 設計（§5.5 能力マトリクス、§8 4権限）
- `D:\アプリ開発\CoreMembers\本番化_セットアップ手順.md` … 本番化手順
- `D:\アプリ開発\CarFlow\carflow\firestore.rules` … ルールの真実
- `D:\アプリ開発\開発全体メモ.md` … エコシステム全体メモ
